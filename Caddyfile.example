{
    admin 0.0.0.0:2019

    log {
        output file /var/log/caddy/access.log {
            roll_size 10MB
            roll_keep 3
        }
        level INFO
        format json
    }
}

:443, *.domain, domain {
    tls {
        dns cloudflare cloudflareApiToken
    }

    route {
        # Forward proxy configuration - 必须在最前面
        forward_proxy {
            basic_auth naiveUser naivePasswd
            hide_ip
            hide_via
            probe_resistance
        }

        # Internal API proxy
        reverse_proxy /proxyPath localhost:2333 {
            header_up -Origin
        }

        # Fallback camouflage
        reverse_proxy https://www.catalog.update.microsoft.com {
            header_up Host {upstream_hostport}
            header_up X-Forward-Host {host}
        }
    }
}